<%= form_for @pessoa, :url => {:action => "create"} do |f| %>
<%= f.text_field :doencas %>
<% Doenca.each do |d| %>
<div>
	<%= check_box_tag :doencas, doenca_id, @pessoa.doencas.include?(sc), :nome => 'pessoa[doenca_ids][]' -%>
	<%= label_tag :doencas, d.nome -%>
</div>
<% end -%>
<% end %>
